apiVersion: k8s.keycloak.org/v2beta1
kind: KeycloakRealmImport
metadata:
  name: api-monetization-realm
  namespace: api-monetization-identity
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  labels:
    app.kubernetes.io/name: api-monetization-realm
    app.kubernetes.io/part-of: api-monetization
spec:
  keycloakCRName: api-monetization
  placeholders:
    FREE_CLIENT_SECRET:
      secret:
        name: keycloak-demo-clients
        key: free-client-secret
  realm:
    id: api-monetization
    realm: api-monetization
    displayName: API Monetization
    loginTheme: api-monetization
    enabled: true
    sslRequired: external
    registrationAllowed: true
    # The portable demo has no SMTP prerequisite. Successful local form
    # registrations are marked trusted by the scoped event listener bundled
    # in the custom RHBK image. Production must replace this with real email
    # verification and SMTP delivery.
    verifyEmail: false
    eventsListeners:
      - jboss-logging
      - trusted-registration-email
    loginWithEmailAllowed: true
    duplicateEmailsAllowed: false
    resetPasswordAllowed: true
    bruteForceProtected: true
    accessTokenLifespan: 300
    ssoSessionIdleTimeout: 1800
    clients:
      - clientId: demo-free-client
        name: Demo Subscription Client
        description: Machine identity mapped to Demo Company in the monetization control plane
        enabled: true
        protocol: openid-connect
        publicClient: false
        clientAuthenticatorType: client-secret
        secret: ${FREE_CLIENT_SECRET}
        serviceAccountsEnabled: true
        standardFlowEnabled: false
        directAccessGrantsEnabled: false
        fullScopeAllowed: false
        protocolMappers:
          - name: api-audience
            protocol: openid-connect
            protocolMapper: oidc-audience-mapper
            config:
              included.custom.audience: api-monetization
              access.token.claim: "true"
  resources:
    requests:
      cpu: 250m
      memory: 1Gi
    limits:
      cpu: "1"
      memory: 2Gi

apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: subscriptions-db-password
  namespace: api-monetization-data
  labels:
    app.kubernetes.io/part-of: api-monetization
spec:
  length: 40
  digits: 8
  symbols: 8
  symbolCharacters: -_!@#%
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: subscriptions-db-credentials
  namespace: api-monetization-data
  labels:
    app.kubernetes.io/part-of: api-monetization
spec:
  refreshPolicy: CreatedOnce
  target:
    name: subscriptions-db-credentials
    creationPolicy: Orphan
    immutable: true
    template:
      engineVersion: v2
      type: kubernetes.io/basic-auth
      data:
        username: monetization_app
        password: "{{ .password }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: subscriptions-db-password


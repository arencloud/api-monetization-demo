apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
rules:
  - apiGroups:
      - route.openshift.io
    resources:
      - routes
    verbs:
      - get
      - list
      - watch
      - delete
      - patch
  - apiGroups:
      - ""
    resources:
      - services
    verbs:
      - get
      - list
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: api-monetization-route-hosts
subjects:
  - kind: ServiceAccount
    name: api-monetization-route-hosts
    namespace: api-monetization-gateway
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-apps
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
rules:
  - apiGroups:
      - gateway.networking.k8s.io
    resources:
      - httproutes
    verbs:
      - get
      - patch
  - apiGroups:
      - kuadrant.io
    resources:
      - authpolicies
    resourceNames:
      - inventory-jwt
      - payments-jwt
      - ai-chat-jwt
    verbs:
      - get
      - patch
  - apiGroups:
      - ""
    resources:
      - configmaps
    resourceNames:
      - inventory-api-openapi
      - payments-api-openapi
      - ai-chat-api-openapi
    verbs:
      - get
      - patch
  - apiGroups:
      - apps
    resources:
      - deployments
    resourceNames:
      - inventory-api
      - payments-api
      - ai-chat-api
    verbs:
      - get
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-apps
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: api-monetization-route-hosts
subjects:
  - kind: ServiceAccount
    name: api-monetization-route-hosts
    namespace: api-monetization-gateway
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-identity
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
rules:
  - apiGroups:
      - route.openshift.io
    resources:
      - routes
    resourceNames:
      - api-monetization-keycloak
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-identity
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: api-monetization-route-hosts
subjects:
  - kind: ServiceAccount
    name: api-monetization-route-hosts
    namespace: api-monetization-gateway
---
apiVersion: batch/v1
kind: Job
metadata:
  name: api-monetization-route-host-migration
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "9"
  labels:
    app.kubernetes.io/name: api-monetization-route-host-migration
    app.kubernetes.io/part-of: api-monetization
spec:
  activeDeadlineSeconds: 180
  backoffLimit: 2
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app.kubernetes.io/name: api-monetization-route-host-migration
        app.kubernetes.io/part-of: api-monetization
    spec:
      restartPolicy: Never
      serviceAccountName: api-monetization-route-hosts
      containers:
        - name: migrate
          image: registry.redhat.io/openshift4/ose-cli-rhel9:v4.22
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -Eeuo
            - pipefail
            - -c
            - |
              api() {
                timeout -k 2s 8s oc --cache-dir=/tmp/.kube/cache --request-timeout=5s "$@"
              }

              retry_api() {
                local attempt
                for attempt in $(seq 1 6); do
                  if api "$@"; then
                    return 0
                  fi
                  if (( attempt < 6 )); then
                    echo "API request failed on attempt $attempt/6; retrying" >&2
                    sleep 5
                  fi
                done
                return 1
              }

              migrate_route() {
                local route_name=$1
                local old_host=$2
                local old_subdomain=${3:-}
                local current_host
                local current_subdomain

                current_host=$(api get route "$route_name" -o jsonpath='{.spec.host}' 2>/dev/null || true)
                current_subdomain=$(api get route "$route_name" -o jsonpath='{.spec.subdomain}' 2>/dev/null || true)
                if [[ $current_host == "$old_host" || \
                  ( -n $old_subdomain && $current_subdomain == "$old_subdomain" ) ]]; then
                  echo "removing legacy Route $route_name"
                  retry_api delete route "$route_name" --wait=true --timeout=60s
                fi
              }

              migrate_route api-monetization api-monetization.demo
              migrate_route api-monetization-jwt jwt.api-monetization.demo jwt.api-monetization
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: batch/v1
kind: Job
metadata:
  name: api-monetization-oidc-auth-migration
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "19"
  labels:
    app.kubernetes.io/name: api-monetization-oidc-auth-migration
    app.kubernetes.io/part-of: api-monetization
spec:
  activeDeadlineSeconds: 180
  backoffLimit: 2
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app.kubernetes.io/name: api-monetization-oidc-auth-migration
        app.kubernetes.io/part-of: api-monetization
    spec:
      restartPolicy: Never
      serviceAccountName: api-monetization-route-hosts
      containers:
        - name: migrate
          image: registry.redhat.io/openshift4/ose-cli-rhel9:v4.22
          imagePullPolicy: IfNotPresent
          command: [/bin/bash, -Eeuo, pipefail, -c]
          args:
            - |
              for auth_policy in inventory-jwt payments-jwt ai-chat-jwt; do
                jwks_url=$(oc get authpolicy "$auth_policy" \
                  -n api-monetization-apps \
                  -o jsonpath='{.spec.rules.authentication.keycloak.jwt.jwksUrl}' \
                  2>/dev/null || true)
                if [[ -n $jwks_url ]]; then
                  echo "removing legacy jwksUrl from AuthPolicy $auth_policy"
                  oc patch authpolicy "$auth_policy" \
                    -n api-monetization-apps \
                    --type=json \
                    --patch='[{"op":"remove","path":"/spec/rules/authentication/keycloak/jwt/jwksUrl"},{"op":"add","path":"/spec/rules/authentication/keycloak/jwt/issuerUrl","value":"https://keycloak-api-monetization.apps.invalid/realms/api-monetization"}]'
                fi
              done
          resources:
            requests: {cpu: 10m, memory: 64Mi}
            limits: {cpu: 200m, memory: 256Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: [ALL]}
            runAsNonRoot: true
            seccompProfile: {type: RuntimeDefault}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: api-monetization-route-hosts
  namespace: api-monetization-gateway
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "20"
  labels:
    app.kubernetes.io/name: api-monetization-route-hosts
    app.kubernetes.io/part-of: api-monetization
spec:
  activeDeadlineSeconds: 900
  backoffLimit: 2
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app.kubernetes.io/name: api-monetization-route-hosts
        app.kubernetes.io/part-of: api-monetization
    spec:
      restartPolicy: Never
      serviceAccountName: api-monetization-route-hosts
      containers:
        - name: align
          image: registry.redhat.io/openshift4/ose-cli-rhel9:v4.22
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -Eeuo
            - pipefail
            - -c
            - |
              api() {
                timeout -k 2s 8s oc --cache-dir=/tmp/.kube/cache --request-timeout=5s "$@"
              }

              retry_api() {
                local attempt
                for attempt in $(seq 1 6); do
                  if api "$@"; then
                    return 0
                  fi
                  if (( attempt < 6 )); then
                    echo "API request failed on attempt $attempt/6; retrying" >&2
                    sleep 5
                  fi
                done
                return 1
              }

              route_host() {
                local route_name=$1
                local route_namespace=${2:-api-monetization-gateway}
                local host
                local admitted

                for _ in $(seq 1 60); do
                  host=$(api get route "$route_name" -n "$route_namespace" -o jsonpath='{.status.ingress[0].host}' 2>/dev/null || true)
                  admitted=$(api get route "$route_name" -n "$route_namespace" -o jsonpath='{.status.ingress[0].conditions[?(@.type=="Admitted")].status}' 2>/dev/null || true)
                  if [[ -n $host && $admitted == True ]]; then
                    printf '%s' "$host"
                    return 0
                  fi
                  sleep 5
                done

                echo "Route $route_namespace/$route_name was not admitted before the timeout" >&2
                return 1
              }

              bind_routes_to_gateway_service() {
                local gateway_service=""

                for _ in $(seq 1 60); do
                  gateway_service=$(api get services \
                    -l gateway.networking.k8s.io/gateway-name=api-monetization,gateway.networking.k8s.io/gateway-class-name=istio \
                    -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || true)
                  [[ -n $gateway_service ]] && break
                  sleep 5
                done
                if [[ -z $gateway_service ]]; then
                  echo "Gateway controller did not create a Service before the timeout" >&2
                  return 1
                fi

                for route_name in api-monetization api-monetization-jwt; do
                  echo "binding OpenShift Route $route_name to Service $gateway_service"
                  retry_api patch route "$route_name" --type=merge \
                    --patch "{\"spec\":{\"to\":{\"name\":\"$gateway_service\"}}}"
                done
              }

              align_route() {
                local host=$1
                local http_route=$2

                echo "aligning HTTPRoute $http_route with https://$host"
                retry_api patch httproute "$http_route" \
                  -n api-monetization-apps \
                  --type=merge \
                  --patch "{\"spec\":{\"hostnames\":[\"$host\"]}}"
              }

              align_oidc_issuer() {
                local issuer=$1
                local auth_policy

                for auth_policy in inventory-jwt payments-jwt ai-chat-jwt; do
                  echo "aligning AuthPolicy $auth_policy with OIDC issuer $issuer"
                  retry_api patch authpolicy "$auth_policy" \
                    -n api-monetization-apps \
                    --type=merge \
                    --patch "{\"spec\":{\"rules\":{\"authentication\":{\"keycloak\":{\"jwt\":{\"issuerUrl\":\"$issuer\"}}}}}}"
                done
              }

              publish_openapi() {
                local api_host=$1
                local jwt_host=$2
                local product=$3
                local configmap="${product}-api-openapi"
                local deployment="${product}-api"
                local openapi_document=""
                local current_urls
                local rollout_state
                local generation
                local observed_generation
                local desired_replicas
                local updated_replicas
                local available_replicas

                for _ in $(seq 1 60); do
                  openapi_document=$(api get configmap "$configmap" \
                    -n api-monetization-apps \
                    -o go-template='{{index .data "openapi.yaml"}}' 2>/dev/null || true)
                  [[ -n $openapi_document ]] && break
                  sleep 5
                done
                if [[ -z $openapi_document ]]; then
                  echo "ConfigMap $configmap was not ready before the timeout" >&2
                  return 1
                fi

                current_urls=$(sed -n 's/^  - url: //p' <<<"$openapi_document")
                if grep -Fxq "https://$api_host" <<<"$current_urls" &&
                  grep -Fxq "https://$jwt_host" <<<"$current_urls"; then
                  echo "OpenAPI server URLs already publish both authentication endpoints"
                  return 0
                fi

                awk -v api_url="https://$api_host" -v jwt_url="https://$jwt_host" '
                  !replaced && /^  - url: / {
                    print "  - url: " api_url
                    print "  - url: " jwt_url
                    replaced=1
                    next
                  }
                  replaced && /^  - url: / {next}
                  {print}
                ' <<<"$openapi_document" >/tmp/openapi.yaml
                if ! grep -Fxq "  - url: https://$api_host" /tmp/openapi.yaml ||
                  ! grep -Fxq "  - url: https://$jwt_host" /tmp/openapi.yaml; then
                  echo "error: OpenAPI document has no replaceable server URL" >&2
                  return 1
                fi
                {
                  printf 'data:\n'
                  printf '  openapi.yaml: |-\n'
                  sed 's/^/    /' /tmp/openapi.yaml
                } > /tmp/openapi-patch.yaml

                echo "publishing OpenAPI server URLs https://$api_host and https://$jwt_host"
                retry_api patch configmap "$configmap" \
                  -n api-monetization-apps \
                  --type=merge \
                  --patch-file=/tmp/openapi-patch.yaml
                retry_api rollout restart deployment/"$deployment" \
                  -n api-monetization-apps

                for _ in $(seq 1 60); do
                  rollout_state=$(api get deployment "$deployment" \
                    -n api-monetization-apps \
                    -o jsonpath='{.metadata.generation}{"|"}{.status.observedGeneration}{"|"}{.spec.replicas}{"|"}{.status.updatedReplicas}{"|"}{.status.availableReplicas}' \
                    2>/dev/null || true)
                  IFS='|' read -r generation observed_generation desired_replicas updated_replicas available_replicas <<<"$rollout_state"
                  if [[ -n $generation && $observed_generation == "$generation" && \
                    $updated_replicas == "$desired_replicas" && \
                    $available_replicas == "$desired_replicas" ]]; then
                    echo "$deployment is serving the updated OpenAPI document"
                    return 0
                  fi
                  sleep 5
                done

                echo "$deployment rollout did not complete before the timeout" >&2
                return 1
              }

              bind_routes_to_gateway_service
              api_hostname=$(route_host api-monetization)
              jwt_hostname=$(route_host api-monetization-jwt)
              keycloak_hostname=$(route_host api-monetization-keycloak api-monetization-identity)
              align_oidc_issuer "https://$keycloak_hostname/realms/api-monetization"
              align_route "$api_hostname" inventory-api-key
              align_route "$jwt_hostname" inventory-jwt
              align_route "$api_hostname" payments-api-key
              align_route "$jwt_hostname" payments-jwt
              align_route "$api_hostname" ai-chat-api-key
              align_route "$jwt_hostname" ai-chat-jwt
              align_route "$api_hostname" ai-chat-api-key-preflight
              align_route "$jwt_hostname" ai-chat-jwt-preflight
              publish_openapi "$api_hostname" "$jwt_hostname" inventory
              publish_openapi "$api_hostname" "$jwt_hostname" payments
              publish_openapi "$api_hostname" "$jwt_hostname" ai-chat
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault

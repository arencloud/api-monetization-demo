# Dashboards

Dashboards will be managed as code after the observability operator and storage
topology are selected. The first dashboard set will cover:

- request volume, status, latency, and bandwidth by API;
- authentication failures and HTTP 429 responses;
- customer and plan usage without exposing credentials;
- billable usage and estimated revenue; and
- gateway, Authorino, Limitador, and backend trace correlation.


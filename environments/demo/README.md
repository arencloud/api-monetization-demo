# Demo environment

This overlay will compose the single-cluster live-demo profile. It will contain
only non-secret environment configuration and references to externally managed
secrets. The runbook will include deterministic seed data, traffic generation,
plan upgrade, verification, and reset procedures.

